# PostgreSQL Database Configuration
# ===================================
# Rename this file to .env and fill in your credentials

# Database connection settings
DB_HOST=localhost
DB_PORT=7000
DB_NAME=BRD
DB_USER=postgres
DB_PASSWORD=root

# Server settings
PORT=3001
NODE_ENV=development

# Optional: Full connection string (overrides individual settings if provided)
DATABASE_URL=postgresql://postgres:root@localhost:7000/BRD
